# This configuration file is based on 'Default' configuration of project 'Maps'

# Database configuration
runtime.params {
  DatabaseType = HSQLDB
  DatabaseName = default
}

# Server configuration
admin {
  port = 8090
  addresses = [ localhost ]
}

runtime {
  http {
    port = 8080
    addresses = [ "*" ]
  }
  params {
    ApplicationRootUrl = "http://localhost:8080/"
  }
}

# Constants configuration
runtime.params.MicroflowConstants {
  # no overrides specified, using default values
}

# Custom configuration
runtime.params {
  # no runtime settings specified
}

# Headers configuration
runtime.params.Headers {
  # no headers specified
}

# Tracing configuration
runtime.tracing {
  # tracing is not configured
}


